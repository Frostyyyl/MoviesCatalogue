<Window x:Class="GrobelnyKasprzak.MovieCatalogue.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:GrobelnyKasprzak.MovieCatalogue.WPF.Views"
        Title="Movie Catalogue"
        Height="700"
        Width="1200"
        Background="#FFFAFAFA"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryYellow" Color="#FFC107"/>
        <SolidColorBrush x:Key="DarkText" Color="#333333"/>
        <SolidColorBrush x:Key="BgCream" Color="#FFFAFAFA"/>

        <Style TargetType="TabItem">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="25,12"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="TabBorder" 
                                BorderThickness="0,0,0,4" 
                                BorderBrush="Transparent" 
                                Background="Transparent"
                                Margin="5,0">
                            <ContentPresenter x:Name="ContentSite"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              ContentSource="Header"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{StaticResource PrimaryYellow}"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryYellow}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FFD54F"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="20,10">
            <TextBlock Text="MOVIE CATALOGUE" 
                       FontSize="20" 
                       FontWeight="ExtraBlack" 
                       Foreground="{StaticResource PrimaryYellow}" />
        </Border>

        <TabControl Grid.Row="1" 
                    Background="Transparent" 
                    BorderThickness="0" 
                    Margin="10">

            <TabItem Header="MOVIES">
                <views:MoviesView DataContext="{Binding MoviesVM}" />
            </TabItem>

            <TabItem Header="DIRECTORS">
                <views:DirectorsView DataContext="{Binding DirectorsVM}" />
            </TabItem>

        </TabControl>
    </Grid>
</Window>